package com.amb8657.websitebuilder;

/** Resize handles exposed by the visual selection overlay. */
public enum ResizeHandle {
    TOP_LEFT, TOP, TOP_RIGHT,
    LEFT, RIGHT,
    BOTTOM_LEFT, BOTTOM, BOTTOM_RIGHT
}
